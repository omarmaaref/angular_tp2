import { HoverableClickableDirective } from './hoverable-clickable.directive';

describe('HoverableClickableDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverableClickableDirective();
    expect(directive).toBeTruthy();
  });
});
